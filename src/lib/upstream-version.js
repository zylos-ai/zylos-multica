/**
 * Latest official Multica CLI capability level implemented by this bridge.
 *
 * This is intentionally independent of the zylos-multica package version.
 * Bump it only after the matching upstream business semantics are implemented
 * and verified against the self-hosted server contract.
 */
export const UPSTREAM_VERSION = '0.2.21';
